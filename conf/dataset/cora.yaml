# @package _group_
name: 'cora'
_target_: examples.datasets.cora.CoraNeighborSamplerDataset
params:
  num_layers: ${model.params.num_layers}
  transform:
    - _target_: torch_geometric.transforms.NormalizeFeatures
